version = 2
name = "tig"
root = "tig"

[detect]
argv0 = ["tig"]
argv_contains = ["tig"]

# tig layout: title-bar at top, main scrollable view, status-line at bottom.
[[regions]]
id = "title"
role = "title-bar"
rows = [0, 0]

[[regions]]
id = "body"
role = "buffer"
rows = [1, -2]

[[regions]]
id = "status"
role = "status-bar"
rows = [-1, -1]
