[package]
name = "agent-tui-engine-alacritty"
description = "alacritty-terminal-backed implementation of the agent-tui Engine trait (lean alternative)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
agent-tui-engine = { workspace = true }
agent-tui-protocol = { workspace = true }
tokio = { workspace = true }
thiserror = { workspace = true }
# Substrate flipped from wezterm-term to alacritty_terminal in P0a: wezterm-term
# is not published on crates.io, which blocks the cargo install / npm / brew
# distribution promise (ADR-TA-012). See LEARN-TA-005.
alacritty_terminal = { version = "0.26", default-features = false }
