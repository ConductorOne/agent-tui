# Rust / Cargo
/target/
**/target/
**/*.rs.bk
Cargo.lock.bak

# Coverage output (cargo-llvm-cov; produced by `cargo llvm-cov --lcov
# --output-path lcov.info` and the CI coverage job)
/lcov.info

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
*~

# Local env
.env
.envrc.local
*.log

# Diagnostic bundles (agent-tui doctor --diagnostic-bundle)
diag-*.tar.gz
diag-*.tar

# Local socket / state dirs accidentally tracked
.agent-tui/
