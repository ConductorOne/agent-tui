# Proper nouns / product names that `clippy::doc_markdown` would otherwise flag
# as un-backticked "code". `..` keeps clippy's built-in default list.
doc-valid-idents = ["ConPTY", "ConductorOne", ".."]
